<?php 
 /*
    A function is a block of code that performs a specific task. It is designed to be reusable, meaning you can call the function multiple times without rewriting the same code. Functions help you organize your code, make it easier to read, and reduce errors.

    Why Use Functions? 
    - Reusability: Write once, use many times.

    - Modularity: Break down complex tasks into smaller, manageable parts.

    - Maintainability: Easier to debug and update code.

    - Readability: Makes your code cleaner and easier to understand.

    The basic syntax of a PHP function is:

        function functionName(parameter1, parameter2, ...) {
        Code to execute
        return $result; // Optional
        }
 */